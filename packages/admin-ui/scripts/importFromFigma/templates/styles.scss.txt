@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    // Background colors.
    {BACKGROUND_COLOR}

    // Border color.
    {BORDER_COLOR}

    // Border radius.
    {BORDER_RADIUS}

    // Border width.
    {BORDER_WIDTH}

    // Fill.
    {FILL}

    // Font.
    {FONT}

    // Margin.
    {MARGIN}

    // Padding.
    {PADDING}

    // Shadow.
    {SHADOW}

    // Spacing.
    {SPACING}

    // Text color.
    {TEXT_COLOR}
  }
}
